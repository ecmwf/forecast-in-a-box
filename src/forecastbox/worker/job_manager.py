"""
Keeps track of locally-spawned processes which run individual jobs
"""

from forecastbox.api.controller import JobDefinition


def job_submit(job_id: str, definition: JobDefinition) -> bool:
	# TODO
	return True
