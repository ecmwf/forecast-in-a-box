Done:
  * 00-overview.md -- Initial overview and task breakdown written
  * 01-add-jobs2-db-plumbing.md -- Added `sqlite_jobs2db_path` to `DatabaseSettings` in `config.py`, created `backend/src/forecastbox/db/jobs2.py` with an empty `Base` and `create_db_and_tables()`, and added `backend/tests/integration/test_db_startup.py`; the new db is auto-discovered by the existing startup loop and created at `{FIAB_ROOT}/jobs2.db` alongside `user.db` and `job.db`.
  * 02-add-jobs2-schema-and-crud.md -- Created `backend/src/forecastbox/schemas/jobs2.py` with five ORM models (`JobDefinition`, `ExperimentDefinition`, `JobExecution`, `GlobalDefaults`, `ExperimentNext`) using composite PKs `(id, version)` / `(id, attempt_count)` and composite FK constraints; updated `backend/src/forecastbox/db/jobs2.py` to import from the schema and add full insert/get/list/update-runtime/soft-delete helpers for each table; added 12 unit tests in `backend/tests/unit/test_jobs2.py` covering latest-version, latest-attempt, soft-delete, and list semantics; note that `ExperimentDefinition` FK constraints reference `JobDefinition(id, version)` but SQLite does not enforce them by default, so callers must supply valid values.
  * 03-add-fable-save-and-retrieve-v2.md -- Added `FableSaveV2Request`, `FableSaveV2Response`, and `FableRetrieveV2Response` Pydantic models to `backend/src/forecastbox/api/types/fable.py`; added `POST /api/v1/fable/upsert_v2` and `GET /api/v1/fable/retrieve_v2` endpoints to `backend/src/forecastbox/api/routers/fable.py` backed by `db_jobs2.upsert_job_definition` / `get_job_definition` with `source=user_defined`; added 4 integration tests in `backend/tests/integration/test_fable_v2_save.py`; existing `/upsert` and `/retrieve` endpoints are unchanged.
  * 04-add-fable-compile-v2.md -- Added `FableCompileV2Request` Pydantic model to `backend/src/forecastbox/api/types/fable.py`; added `PUT /api/v1/fable/compile_v2` endpoint to `backend/src/forecastbox/api/routers/fable.py` that loads a `JobDefinition` from the jobs2 store by id/version and passes its builder through the existing `api_fable.compile()` machinery, returning a standard `ExecutionSpecification`; added 3 integration tests in `backend/tests/integration/test_fable_v2_compile.py`.
  * 05-add-job-execute-v2.md -- Added `JobExecuteV2Request` (with model_validator enforcing exactly one of job_definition_id or spec) and `JobExecuteV2Response` (execution_id, id, definition_id, definition_version) to `api/types/jobs.py`; added `execute_v2` and `execute_v2_to_response` helpers to `api/execution.py` that resolve/persist a JobDefinition, insert a JobExecution row, call the existing cascade submission path, update runtime fields, and also write to the v1 job.db for status-polling compatibility; added `POST /api/v1/job/execute_v2` endpoint to `api/routers/job.py`; added 5 integration tests in `backend/tests/integration/test_submit_job_v2_execute.py` covering one-off raw spec, saved definition, missing/both fields validation, and unknown definition.
  * 06-add-job-read-and-rerun-v2.md -- Added `JobExecutionStatusV2`, `JobExecutionListV2`, and `JobSpecificationV2` response models to `api/types/jobs.py`; added `get_job_execution_specification_v2` and `restart_job_execution_v2` helpers to `api/execution.py`; removed the v1 `insert_one` mirror from `execute_v2` (v1 write dropped as design now dictates using v2 endpoints for polling); added five new endpoints to `api/routers/job.py` (`GET /status_v2`, `GET /{id}/status_v2`, `GET /{id}/outputs_v2`, `GET /{id}/specification_v2`, `POST /{id}/restart_v2`) with cascade polling and latest-attempt defaulting; added 8 integration tests in `backend/tests/integration/test_submit_job_v2_read.py`.

Not done:
  * 07-add-schedule-persistence-v2.md
  * 08-add-scheduler-runtime-and-runs-v2.md
