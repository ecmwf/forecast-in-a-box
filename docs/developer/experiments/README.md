This folder contains summary of code change experiments -- usually of the sort of "replacing one thing with another".

If an experiment is succesful, no trace of it is left here, instead it is merged into the main codebase.
Otherwise, the reasons of failure are summarized here, so that it is not needlessly repeated.
