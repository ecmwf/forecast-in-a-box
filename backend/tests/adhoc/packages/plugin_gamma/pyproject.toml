[build-system]
requires = ["setuptools>=70"]
build-backend = "setuptools.build_meta"

[project]
name = "fiab-adhoctest-plugin-gamma"
version = "1.0.0"
requires-python = ">=3.10"
dependencies = ["fiab-core", "fiab-adhoctest-pseudonumpy==2.0.0"]

[tool.setuptools.packages.find]
where = ["src"]
