[build-system]
requires = ["setuptools>=70"]
build-backend = "setuptools.build_meta"

[project]
name = "fiab-adhoctest-plugin-beta"
version = "1.0.0"
requires-python = ">=3.10"
dependencies = ["fiab-core", "fiab-adhoctest-widget==1.0.0"]

[tool.setuptools.packages.find]
where = ["src"]
